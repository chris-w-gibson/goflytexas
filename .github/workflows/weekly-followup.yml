name: Weekly Lead Follow-up

on:
  schedule:
    # Daily at 14:00 UTC = 9:00 AM CT during CDT, 8:00 AM CT during CST.
    # The endpoint runs the staggered drip (day 7 / 14 / 21 after the lead came
    # in, see FOLLOWUP_DAYS); each step sends once per lead, so daily is safe.
    - cron: '0 14 * * *'
  workflow_dispatch: {}

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: POST /api/cron/weekly-followup
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          APP_URL: ${{ vars.APP_URL || 'https://goflytexas-production.up.railway.app' }}
        run: |
          set -e
          response=$(curl -fsS -X POST \
            -H "Authorization: Bearer $CRON_SECRET" \
            -w '\n%{http_code}' \
            "$APP_URL/api/cron/weekly-followup")
          body=$(printf '%s' "$response" | sed '$d')
          code=$(printf '%s' "$response" | tail -n1)
          echo "HTTP $code"
          echo "$body"
          if [ "$code" != "200" ]; then exit 1; fi
